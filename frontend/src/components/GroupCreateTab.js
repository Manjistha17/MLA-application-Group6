import React from "react";
import GroupCreate from "./GroupCreate";

const GroupCreateTab = ({ currentUser }) => {
  return <GroupCreate currentUser={currentUser} />;
};

export default GroupCreateTab;
